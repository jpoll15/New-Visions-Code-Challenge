import React from 'react'
import ReactDOM from 'react-dom'
import App from './components/App'

// tell React where in the DOM to add the App component
ReactDOM.render(<App />, document.getElementById('app'))
